import Layout from "./Layout";
import RegisterForm from "./RegisterForm";

const Register = () => {
  return (
    <Layout headTitle="新規登録" title="Register your account">
      <RegisterForm />
    </Layout>
  );
};

export default Register;
