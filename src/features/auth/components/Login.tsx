import Layout from "./Layout";
import LoginForm from "./LoginForm";

const Login = () => {
  return (
    <Layout headTitle="ログイン" title="Log in to your account">
      <LoginForm />
    </Layout>
  );
};

export default Login;
